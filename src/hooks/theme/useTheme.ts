import { getTheme } from "src/utils/theme/getTheme";

const useTheme = () => {
  const {} = getTheme();
  
  const themeColor = ()=>{
    
  }

  return {
    themeColor,
  };
};

export default useTheme;
